// Example dashboardReducer implementation
import { createSlice } from '@reduxjs/toolkit';

const dashboardSlice = createSlice({
  name: 'dashboard',
  initialState: {},
  reducers: {
    // define your reducers here
  },
});

export default dashboardSlice.reducer;